# APLOMB. Calm — nausea kit (supplier TBD)

This folder is **intentionally minimal** while supplier selection is in flight.

## Status

Supplier not yet chosen. Once chosen, run the same flow used for the other three SKUs:
1. Capture the supplier's required label dimensions + format → write `SPEC_REQUIREMENTS.md`
2. Add the SKU's spec to `scripts/label_design/specs.py`
3. Run `python3 -m scripts.label_design.artwork nausea` to generate artwork
4. Replace this README with a full "upload here" doc

## Prior research draft

See `_draft_suni_ginger_gummies/` for the full design package generated for the SUNI 180 cc ginger-gummy candidate. That candidate is preserved as a reference; it is not the current direction unless explicitly chosen.
